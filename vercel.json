{
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand": "npm run build:ghpages",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}